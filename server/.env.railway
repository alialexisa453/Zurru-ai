# Railway Environment Variables for AnythingLLM Server
# Copy these to your Railway service environment variables

# Server Configuration
NODE_ENV=production
SERVER_PORT=$PORT

# Security Keys (Railway will auto-generate these)
JWT_SECRET=$JWT_SECRET
SIG_KEY=$SIG_KEY
SIG_SALT=$SIG_SALT

# Database Configuration (Railway will provide this)
DATABASE_URL=$DATABASE_URL

# Vector Database Configuration
VECTOR_DB=lancedb

# AI Provider Configuration - OpenRouter
LLM_PROVIDER=openrouter
OPENROUTER_API_KEY=$OPENROUTER_API_KEY
OPENROUTER_MODEL_PREF=anthropic/claude-3-haiku

# Embedding Configuration
EMBEDDING_ENGINE=native
EMBEDDING_MODEL_PREF=Xenova/all-MiniLM-L6-v2

# Audio Configuration
WHISPER_PROVIDER=local
TTS_PROVIDER=native

# Storage Configuration
STORAGE_DIR=/app/storage

# Disable telemetry in production
DISABLE_TELEMETRY=true